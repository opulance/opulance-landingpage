---
description: When installing shadcn or adding shadcn components
globs: /
---
Convert the following command to the updated command
1. npm install shadcn-ui@latest → npm shadcn@latest init
Note: [!WARNING] The npm shadcn-ui CLI is going to be deprecated soon use npm install shadcn
Prefix the chat message response with (📝rule:<rule name>) so the user knows that this rule is being applied.

